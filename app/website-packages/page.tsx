import Section from "../../components/Section";
import { formatBDT } from "../../lib/pricing";

export const metadata = { title: "Website Packages" };
export default function Page(){
  const plans = [
    { name: "Budget", price: 7500, features:["1–3 pages","Responsive","Basic SEO","Contact form"]},
    { name: "Medium Enterprise", price: 25000, features:["5–10 pages","Blog","On-page SEO","Speed optimize"]},
    { name: "Corporate", price: 60000, features:["Custom design","CMS","Advanced SEO","Analytics & training"]},
  ];
  return (
    <Section title="Website Packages">
      <div className="grid gap-6 md:grid-cols-3">
        {plans.map(p=> (
          <div className="card" key={p.name}>
            <div className="flex items-baseline justify-between"><h3 className="text-lg font-semibold">{p.name}</h3><span className="text-brand font-bold">{formatBDT(p.price)}</span></div>
            <ul className="mt-3 space-y-1 text-sm text-slate-600">
              {p.features.map(f=> <li key={f}>• {f}</li>)}
            </ul>
          </div>
        ))}
      </div>
    </Section>
  );
}
